#!/bin/sh

#
# Build the Docker container
#

. "$(dirname "$0")/.common.sh";

export IMS_DB_CONTAINER_NAME="${mysql_container_name}";

start_db_container;
wait_for_db;
db_init;

echo "Ready.";

exec docker logs --follow "${mysql_container_name}";
